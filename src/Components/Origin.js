import React from "react";

export default function Origin(props) {
  return (
    <>
      <div className="Nationalites">{props.intro}</div>
      <div className="Nom">{props.name}</div>
    </>
  );
}
